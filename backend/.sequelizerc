const path = require("path");

module.exports = {
	config: path.resolve(
		__dirname,
		"database",
		"chat",
		"config",
		"sequelize-cli.config.js"
	),
	"models-path": path.resolve(__dirname, "database", "chat", "models"),
	"migrations-path": path.resolve(
		__dirname,
		"database",
		"chat",
		"migrations"
	),
};
